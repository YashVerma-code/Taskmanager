import React from 'react';

type Props = {
  propName: string;
};

const Home: React.FC<Props> = ({ propName }) => {
  return (
    <div>
      /* Your code here */
    </div>
  );
};

export default Home;